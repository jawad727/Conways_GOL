import React from "react";

const Presets = props => {
  return (
    <div>
      <button onClick={props.bitcoin}>PRESET</button>
      <img src='../imgs/bitcoin.jpg' />
    </div>
  );
};

export default Presets;
